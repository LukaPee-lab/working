<Window x:Class="NexusEditor.WorkspaceSwitchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="워크스페이스 전환"
        Width="620"
        Height="330"
        MinWidth="620"
        MinHeight="330"
        MaxWidth="620"
        MaxHeight="330"
        Icon="Assets/AppIcon.ico"
        Background="#1b1b1b"
        Foreground="#d8d8d8"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="Background" Value="#3a3a3a"/>
            <Setter Property="Foreground" Value="#ededed"/>
            <Setter Property="BorderBrush" Value="#565656"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#4a4a4a"/>
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#6a6a6a"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#303030"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#67a8dd"/>
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsDefaulted" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#67a8dd"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#345f88"/>
            <Setter Property="BorderBrush" Value="#5d8db8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#292929"
                BorderBrush="#111111"
                BorderThickness="0,0,0,1"
                Padding="24,18">
            <TextBlock x:Name="HeaderText"
                       Text="워크스페이스 전환"
                       FontSize="21"
                       FontWeight="SemiBold"
                       Foreground="#f1f1f1"/>
        </Border>

        <Grid Grid.Row="1" Margin="24,24,24,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="54"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Width="40"
                    Height="40"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="#493f2d"
                    BorderBrush="#806e45"
                    BorderThickness="1"
                    CornerRadius="20">
                <TextBlock Text="!"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="#e6c66d"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="HeadlineText"
                           Text="저장되지 않은 변경 내용이 있습니다."
                           FontSize="17"
                           FontWeight="SemiBold"
                           Foreground="#ededed"/>
                <TextBlock x:Name="MessageText"
                           Margin="0,10,0,0"
                           Text="현재 변경 내용을 어떻게 처리할지 선택하세요."
                           FontSize="14"
                           Foreground="#bdbdbd"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2"
                Background="#242424"
                BorderBrush="#111111"
                BorderThickness="0,1,0,0"
                Padding="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="취소"
                        IsCancel="True"
                        Click="CancelButton_Click"/>
                <Button x:Name="DiscardButton"
                        Grid.Column="2"
                        Content="변경 내용 무시"
                        Click="DiscardButton_Click"/>
                <Button x:Name="ExportAndSwitchButton"
                        Grid.Column="4"
                        Content="Export 후 전환"
                        IsDefault="True"
                        Style="{StaticResource PrimaryButton}"
                        Click="ExportAndSwitchButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
